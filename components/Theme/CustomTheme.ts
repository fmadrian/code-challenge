/**
 *  Custom theme to change apperance of MUI components.
 */
import { createTheme } from '@mui/material/styles';

const customTheme = createTheme({
    palette: {
        mode: 'light',
    }
});

export default customTheme;
